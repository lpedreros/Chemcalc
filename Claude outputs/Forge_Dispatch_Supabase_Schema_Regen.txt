You are Forge, Full-Stack Build for ChemCalc. Access: the clear-testing branch (this session's repo). Task touches ONLY these 2 files — do not touch anything else: supabase_schema.sql (repo root) and testing/supabase_schema.sql. They are currently byte-identical; keep them byte-identical after your edit.

Context: both files are a stale setup script — they document only 2 of the 10 tables that actually exist in the live Supabase project, and the 2 they do cover are each missing real columns. This is documentation only; you are not running anything against the live database, not connecting to Supabase, and not changing any table, policy, or function that actually exists. You are rewriting a static .sql file so it accurately reflects live reality, captured directly from the database by Atlas.

Full live schema to encode, verbatim (all 10 tables, public schema):

TABLE profiles (RLS enabled):
  id uuid primary key references auth.users(id) on delete cascade
  created_at timestamptz default now()
  email text
  full_name text
  company_name text
  estimate_prefix text default 'EST'
  logo_url text
  tier text default 'free'
  stripe_customer_id text
  stripe_subscription_id text
  subscription_status text default 'inactive'
  trello_api_key text
  trello_token text
  trello_board_id text
  trello_board_name text
  trello_list_id text
  trello_list_name text
  biz_name text
  biz_tagline text
  biz_phone text
  biz_email text
  biz_website text
  biz_address text
  biz_prefix text
  biz_logo_url text
  beta_tester boolean default false
Policies: "Users can view own profile" SELECT using (auth.uid() = id); "Users can update own profile" UPDATE using (auth.uid() = id).

TABLE estimates (RLS enabled):
  id uuid primary key default gen_random_uuid()
  created_at timestamptz default now()
  updated_at timestamptz default now()
  user_id uuid references auth.users(id) on delete cascade
  company_name text
  estimate_number text
  valid_until date
  customer_first text
  customer_last text
  customer_phone text
  customer_email text
  boat_name text
  boat_make text
  boat_model text
  hin text
  materials_total numeric(10,2) default 0
  paint_total numeric(10,2) default 0
  labor_total numeric(10,2) default 0
  grand_total numeric(10,2) default 0
  hourly_rate numeric(10,2) default 100
  estimate_data jsonb
  status text default 'draft'
  notes text
  trello_card_id text
  trello_synced_at timestamptz
  estimate_name text default ''
Policies: "Users can view own estimates" SELECT using (auth.uid() = user_id); "Users can insert own estimates" INSERT with check (auth.uid() = user_id); "Users can update own estimates" UPDATE using (auth.uid() = user_id); "Users can delete own estimates" DELETE using (auth.uid() = user_id).
Trigger: set_updated_at BEFORE UPDATE, calls update_updated_at() (sets new.updated_at = now()).

TABLE custom_materials (RLS enabled):
  id uuid primary key default gen_random_uuid()
  user_id uuid, references auth.users(id) [FK — confirm on delete behavior isn't documented in live introspection, use ON DELETE CASCADE to match the profiles/estimates pattern unless Leo says otherwise]
  source text not null, check (source in ('custom','affiliate'))
  affiliate_id uuid references public.affiliate_materials(id)
  name text
  url text
  cost numeric default 0
  unit text default 'each'
  markup numeric default 40
  notes text
  is_favorite boolean default false
  created_at timestamptz default now()
  updated_at timestamptz default now()
Policies: "Users can read their own custom materials" SELECT using (auth.uid() = user_id); "Users can insert their own custom materials" INSERT with check (auth.uid() = user_id); "Users can update their own custom materials" UPDATE using (auth.uid() = user_id); "Users can delete their own custom materials" DELETE using (auth.uid() = user_id).

TABLE affiliate_materials (RLS enabled):
  id uuid primary key default gen_random_uuid()
  aff_key text unique
  name text not null
  url text
  section text not null
  tags text[] default '{}'
  is_favorite boolean default false
  created_at timestamptz default now()
  updated_at timestamptz default now()
Policy: "Anyone can read affiliate_materials" SELECT using (true).

TABLE task_presets (RLS enabled):
  id uuid primary key default gen_random_uuid()
  name text not null unique
  category text not null
  icon text
  description text
  scope_steps text[] default '{}'
  material_rows jsonb default '[]'
  paint_rows jsonb default '[]'
  task_rows jsonb default '[]'
  sort_order integer default 0
  is_active boolean default true
  created_at timestamptz default now()
  updated_at timestamptz default now()
Policy: "Public can read task_presets" SELECT using (true).
Trigger: calls update_task_presets_updated_at() before update (same pattern as estimates' set_updated_at).

TABLE user_task_templates (RLS enabled):
  id uuid primary key default gen_random_uuid()
  user_id uuid not null, references auth.users(id)
  name text not null
  scope_steps text
  task_rows jsonb
  created_at timestamptz default now()
Policy: "Users manage own task templates" FOR ALL using (auth.uid() = user_id) with check (auth.uid() = user_id).

TABLE calculator_events (RLS enabled):
  id uuid primary key default gen_random_uuid()
  created_at timestamptz default now()
  calculator text not null
  inputs jsonb default '{}'
  results jsonb default '{}'
  user_id uuid references auth.users(id)
  session_id text
  country text
  city text
  email_captured boolean default false
    -- COMMENT ON COLUMN: 'Analytics flag. UNRELIABLE for rows created before 2026-09-16: anonymous sessions could never flip this (no SELECT policy for anon meant the UPDATE silently matched zero rows, with no error). Only 3 pre-fix rows are true, all from authenticated sessions. Filter on created_at >= 2026-09-16 for meaningful capture rates. Fixed via the mark_email_captured() RPC.'
Policies: "Anyone can log a calculation" INSERT with check (true); "Users can read their own events" SELECT using (auth.uid() = user_id); "Users can update email_captured on their own session" UPDATE using (true) with check (true) [deliberately left loose, superseded in practice by the RPC below — do not tighten this in this dispatch, it's a separate decision].

TABLE tips (RLS enabled, zero policies — intentionally locked, only reachable via get_random_tip()):
  id integer primary key default nextval('tips_id_seq')
  category text not null
  tip text not null
  active boolean default true
  created_at timestamptz default now()

TABLE tip_category_calculators (RLS enabled, zero policies — same lockout, same access path):
  category text
  calculator text
  primary key (category, calculator)

TABLE affiliate_clicks (RLS enabled):
  id uuid primary key default gen_random_uuid()
  product_name text not null
  product_url text not null
  page text not null
  session_id text
  user_id uuid references auth.users(id)
  created_at timestamptz default now()
Policies: "Allow anonymous inserts" INSERT with check (true); "Authenticated users can read" SELECT using (auth.uid() IS NOT NULL).

FUNCTIONS (all in public schema):
  handle_new_user() returns trigger, SECURITY DEFINER — on auth.users insert, creates the matching profiles row (id, email, full_name from raw_user_meta_data).
  update_updated_at() returns trigger — sets NEW.updated_at = now(), used by estimates.
  update_task_presets_updated_at() returns trigger — same pattern, used by task_presets.
  get_random_tip(p_calculator text) returns text, SECURITY DEFINER, SET search_path = public — looks up tip_category_calculators for a mapping to p_calculator; if found, picks a random active tip from that category; if not, falls back to category 'General Pro Tips'.
  mark_email_captured(p_session_id text) returns integer, SECURITY DEFINER, SET search_path = public — finds the most recent calculator_events row for p_session_id, sets email_captured = true, returns 1; returns 0 for null/empty/unmatched session.

Task: rewrite both files (identical content) as a complete, runnable setup script covering all 10 tables above in dependency order (profiles and its trigger first, since other tables reference it and auth.users; then estimates; then the rest), all listed RLS policies, both check constraints noted, all 5 non-trigger-only functions plus their triggers, in the same style as the existing file (create table if not exists, comment headers per section, a closing "DONE" section with any manual dashboard steps still required). Keep the existing profiles/estimates sections' structure and comments where they're still accurate — extend them for the missing columns, don't rewrite what's already correct. Add the calculator_events column comment verbatim via COMMENT ON COLUMN. Do not touch any other file. Do not connect to or run anything against the live Supabase project — this is a static file matching what Atlas already confirmed live via direct database introspection.

CRITICAL — write verification: after editing, commit the change, then independently re-read both files to confirm they're identical to each other and that the file compiles as valid SQL syntax (a local psql --dry-run-style parse check, or at minimum a careful manual read for balanced parens/quotes — there is no live database to test against safely, so syntax correctness is the bar here, not execution).

Commit message to use:

docs: regenerate supabase_schema.sql to match live schema (10 tables, all RLS policies and functions)

Doc previously covered 2 of 10 live tables with stale columns on both.
No live DB changes — static setup-script file only.

Report: file-by-file diff summary, confirmation both files stay identical, and push status checked against origin at the moment you checked it.
